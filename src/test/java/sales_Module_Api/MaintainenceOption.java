package sales_Module_Api;

import org.testng.annotations.Test;

import io.restassured.builder.RequestSpecBuilder;
import io.restassured.specification.RequestSpecification;

public class MaintainenceOption {
	
	
	
	@Test
	public void maintainenceOption()
	
	
	{/*
		 * RestAssured.baseURI ="https://sales-api-dev.colanapps.in";
		 * given().log().all().header("authorization" ,Authentication.Bearer())
		 * .when().get("/api/core/v1/MaintenanceOption/GetAll")
		 * .then().log().all().assertThat().statusCode(200);
		 * 
		 * // 2 . Post Method . given().log().all().header("authorization",)
		 */
		// Specbuilder Concept
		
	//RequestSpecification request =	new RequestSpecBuilder().setBaseUri("https://sales-api-dev.colanapps.in")
	
		
		
	}

}

